const  myObject ={
     js:'javascript',
     cpp:'c++'
}
for (const key in myObject) {           //for in loop  (used in objects)
    // console.log(key)
    console.log(myObject[key])          //myobject ki sari key ki value de do 

    //arays ke liye for in loop

    const programming =["a","b","c","d"]
    for (const key in programming) {
        console.log(key);                   // iske andar arrays ki keys(index) a gye 
        console.log(programming[key]);      // values print karvane ke liye
        
        }
    }

     

